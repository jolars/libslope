@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Eigen3 3.4 REQUIRED NO_MODULE)

if(@USE_OPENMP@)
  find_dependency(OpenMP)
endif()

if(NOT TARGET slope::slope)
  include("${CMAKE_CURRENT_LIST_DIR}/slopeTargets.cmake")
endif()

check_required_components(slope)
